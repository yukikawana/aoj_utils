#include <stdio.h>
#include "utils.h"
using namespace std;

int main() {
    int N, i;
    input(N);
    int A[N];
    for (i = 0; i < N; i++) input(A[i]);
    print_array(N, A);
    

    
    return 0;
    }
